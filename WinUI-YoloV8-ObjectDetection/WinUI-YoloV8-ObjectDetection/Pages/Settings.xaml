<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUI_YoloV8_ObjectDetection.Pages.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_YoloV8_ObjectDetection.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollView Margin="20">
            <StackPanel Spacing="10">
                <TextBlock Text="Select Execution Provider" Style="{ThemeResource SubheaderTextBlockStyle}"/>
                <DropDownButton Content="{Binding ExecutionProvider, Mode=TwoWay}">
                    <DropDownButton.Flyout>
                        <MenuFlyout x:Name="ExecutionProviderMenuFlyout"/>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
